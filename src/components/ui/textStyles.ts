import { defineTextStyles } from "@chakra-ui/react";

export const textStyles = defineTextStyles({
    h1: {
        value: {
            fontSize: '3rem',
            lineHeight: "1.5"
        }
    }
})